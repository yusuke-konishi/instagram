<div class="container">
  <div class="col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <h2>写真を投稿する</h2>
    <div class="clearfix">
      <p class="pull-left" id="profile-img"><%= profile_img(current_user) if profile_img(current_user) %></p>
      <p class="text-left"><%= current_user.name %></p>
    </div>
    <%= render 'form' %>
  </div>
</div>
